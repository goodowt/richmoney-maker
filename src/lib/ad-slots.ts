/**
 * 애드센스 수동 광고 단위 슬롯 ID.
 * AdSense > 광고 > 광고 단위 > 디스플레이 광고(반응형)에서 만든 뒤,
 * 코드 스니펫의 data-ad-slot 숫자를 여기에 넣으면 됩니다.
 */
export const adSlots = {
  /** 계산 결과 바로 아래 (사용자가 결과를 확인한 직후, 체류 시간이 가장 높은 지점) */
  afterResult: "",
  /** FAQ 다음, 푸터 위 (콘텐츠를 끝까지 읽은 고관여 사용자를 잡는 지점) */
  pageBottom: "3563585845",
} as const;
